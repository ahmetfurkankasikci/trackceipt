{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    /*
      Bu iki satırı ekleyin.
      'experimentalDecorators', @inject gibi decorator'ların kullanımına izin verir.
      'emitDecoratorMetadata', tsyringe'in enjekte edilecek tipleri (class, interface vb.)
      anlaması için gereklidir.
    */
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}